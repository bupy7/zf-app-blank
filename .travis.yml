language: php
php:
  - '7.1'
  - '7.2'

before_script: bash ./travis/before-script.sh

script:
  # run tests
  - composer test:build
  - composer test:run

before_install: bash ./travis/before-install.sh
