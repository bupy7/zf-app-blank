language: php
php:
  - '7.1'
  - '7.2'

dist: trusty

before_install: sudo bash ./travis/before-install.sh

before_script: bash ./travis/before-script.sh

script:
  # run tests
  - composer test:build
  - composer test:run
