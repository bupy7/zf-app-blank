var App = App || {};

(function () {
    'use strict';

    App.EnLocale = {
        translation: {
            ERROR_VALIDATOR_REQUIRED: "Value is required and can't be empty",
            ERROR_VALIDATOR_FORMAT_EMAIL: 'Invalid Email address',
            ERROR_VALIDATOR_MIN_LENGTH: 'The input is less than [[= exp ]] characters long',
            ERROR_VALIDATOR_MAX_LENGTH: 'The input is more than [[= exp ]] characters long'
        }
    };
})();
